# 桌面打包依赖(Windows 构建用)
# 相比 backend/requirements.txt:
#   - 去掉 passlib(已弃用,改用 bcrypt 直调)
#   - 去掉 psycopg(桌面用 SQLite,不需要 PG 驱动,且避免 Windows 编译)
#   - 去掉 minio/boto3(桌面用本地文件存储,无需 S3 客户端)
#   - 去掉 alembic(桌面用 create_all 建表,migration 在 SQLite 不可用)
#   - 新增 bcrypt / pywebview / pyinstaller
fastapi
uvicorn[standard]
sqlalchemy>=2.0
pydantic>=2
pydantic-settings
bcrypt
python-jose[cryptography]
python-multipart
httpx
pdfplumber
pymupdf
pillow
python-dotenv
openpyxl
python-docx
pywebview
pyinstaller
